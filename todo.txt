FIGHT GLARE — TODO
Updated 2026-08-27

DONE
  [x] Repo created, site pushed to github.com/arphysics/fight_glare_movement
  [x] GitHub Pages deploying from main on every push
  [x] SITE IS LIVE at https://fightglare.org
  [x] GoDaddy DNS: four GitHub A records on @, parking record deleted
  [x] GoDaddy DNS: www CNAME -> arphysics.github.io
  [x] Custom domain registered in repo Settings -> Pages

  [x] Enforce HTTPS on — http:// 301s to https://
  [x] Cert covers both fightglare.org and www.fightglare.org (Let's Encrypt,
      auto-renews, expires 2026-10-23)

  Deployment is fully done. All four URL variants (http/https x apex/www)
  return 200 and the live page matches the committed index.html.

BLOCKING SHARING THE LINK
  [x] PETITION_URL set to the change.org petition. All four buttons navigate;
      opens in a new tab. Tracking param (?source_location=...) stripped — it
      was an artifact of where the link was copied from, not part of the link.
  [x] Contact address is fightglareirvine@gmail.com, in the CTA block and the
      footer. Went with a real mailbox instead of setting up GoDaddy forwarding
      for hello@fightglare.org — no DNS/MX work, and it can't silently break.
      Also removed the visible "update email" placeholder badge that had been
      showing on the live page next to it.

  Nothing is blocking sharing the link now.

  If a branded address is ever wanted, GoDaddy forwarding for
  hello@fightglare.org is still the path — it would land in the same mailbox.

NEXT SESSION — START HERE
  [x] Her push doesn't work — SOLVED, and it was never her machine.
      A cloud session hit the identical failure with no Mac and no local git
      credentials involved, which rules out the local-credentials theory this
      entry was already sceptical of. The verbatim error this note said had
      never been seen:
        Permission to arphysics/fight_glare_movement.git denied to arphysics
      (git-receive-pack 403, git-upload-pack 200 — reads fine, writes refused.)
      The GitHub App reported "Resource not accessible by integration" on
      create_branch and create_pull_request too. Aditya fixed it by changing
      repo permissions mid-session; pushes have worked since.
      So: have her retry. If it fails again it is repo permissions, not her.

  [x] Two claims from her copy went live UNSOURCED — both gone.
      The "As of July 2026" date and the 1800-2400K range were in the passage
      the Aug 2026 copy pass rewrote. Neither string is in index.html now.

  [x] Copy nits in the goal list — fixed. Bullets are "Label: ..." instead of
      "Label — is ...", so the lead-in no longer supplies a doubled "is", and
      "not a whit more" is gone.

  [ ] NEW, same rule, same risk: the cost answer and the council letter both
      assert things about SCE and about Irvine with nothing to link.
        - "Edison offers 2700K, 3000K, or 4000K for the same price"
        - "Irvine chose the harsh 4000K light when the warm option was offered
          free of charge"
      PROVENANCE (Aditya, Aug 2026): both come from a meeting with city staff.
      So the claims are believed true, but the source is verbal and therefore
      not linkable — which is exactly what CLAUDE.md's rule is about.
      Claude checked what it could and got nowhere: SCE does publish three
      colour temperatures and Ventura adopted 2700K as "the warmest colour
      offered by SCE", so the choice claim is plausible, but the no-price-
      difference point and Irvine's own 4000K decision could not be confirmed.
      sce.com, docs.cpuc.ca.gov and every city-document host are blocked by the
      sandbox firewall, so that check was inconclusive, not negative.
      Options, best first:
        1. Find it in writing — a follow-up email, meeting notes, a staff
           report, or the LS-1 election form. Makes it checkable, like the AMA
           line. Worth one email to ask.
        2. Attribute in the copy: "City staff have confirmed to us that ..."
           Honest about provenance, survives a challenge, needs no URL.
        3. Leave as bare assertion. Weakest, and it is the page's strongest
           argument — worth more than that.
      Do NOT name the individual staff member, here or on the page. They spoke
      in a meeting, not for publication, and the campaign is in ongoing dialogue
      with the city. "City staff" carries the same weight without costing that
      relationship. NOTE this file is itself published — deploy.yml uploads the
      repo root, so todo.txt is served at fightglare.org/todo.txt. Anything
      written here is public.

CONTENT / POLISH
  [x] Proper og:image — assets/og-image.png, 1200x630, generated from
      assets/og-card.html. NOTE the committed PNG was rendered where webfonts
      were unreachable, so its headline is a Georgia fallback, not Fraunces.
      Regenerate locally (command is in og-card.html's header comment) before
      sharing the link widely — platforms cache scraped cards for a long time.
  [x] og:description rewritten from the current hero copy. Added
      og:image:width/height and twitter:card=summary_large_image so the card
      renders wide rather than as a thumbnail.
  [x] Removed unused assets — owl-logo-v1.png and IMG_3840.png. 322KB.
  [ ] Decide whether fonts-preview.html stays in the repo — it's a scratch
      page, publicly reachable at /fonts-preview.html. More pointed now: it was
      rebuilt into a six-option heading-font comparison, so it is a bigger
      public page than it was.
  [x] Hero H1 tracking relaxed from -1.5px to -0.5px — the value
      fonts-preview.html uses for every serif option. NOT seen rendered in
      Fraunces; worth a look.
  [x] Edison / 4000K claims now attributed: "City staff have confirmed to us
      that ..." in the cost answer and the council letter, and "By the city's
      own account ..." on the 4000K decision. Still worth getting in writing
      and linking it — attribution survives a challenge, a link ends one.
  [x] Contributor docs no longer published. deploy.yml strips todo.txt,
      CLAUDE.md, EDITING.md and the two launch scripts from the Pages artifact
      before upload, with a guard step that fails the build if index.html,
      CNAME or the logo went missing. The files stay in git; only the
      published copy changes. Cloudflare builds separately and is unaffected.
      STILL PUBLIC by choice: README.md, fonts-preview.html (see above),
      _headers and wrangler.jsonc (Cloudflare needs them).

AUG 2026 COPY + DESIGN PASS — MERGED to main via PR #3, live on fightglare.org
  Branch was claude/irvine-streetlight-standards-21g9cy.
  Work continues; the open items above apply to the live site now, not to a
  branch nobody can see.

  [x] Two rounds of copy rewrites through hero, problem, goal, facts, Q&A,
      council letter, and the take-action steps
  [x] CALGreen paragraph corrected — it had been rewritten to say 3000K was an
      "absolute ceiling". It is a voluntary tier measure, and the street-
      lighting carve-out is the reason a city ordinance is needed at all
  [x] YouTube Short embedded beside the three R's, which stack into a column of
      matching height. youtube-nocookie, lazy-loaded, 9/16 frame
  [x] "Join our team" added as an emphasised fifth action step
  [x] Footer legal disclaimer
  [x] Council letter is now a mail merge — name/street/email fill the sign-off,
      the clipboard copy, and the mailto body from one set of fields
  [x] Heading font Roboto Mono -> Fraunces (a reviewer read mono as futuristic)
  [x] Palette rebuilt from colours sampled out of assets/owl-logo.png, incl.
      the ~36 hardcoded rgba() values that were shades of the old tokens
  [x] Page background is the sky INSIDE the medallion (#2a2f3e), not the disc
      shadow. First attempt used the shadow, which is near-black like the old
      background, so the change was invisible and read as "colours never
      changed". Matching the interior let brightness(0.9), the heavy rim
      vignette, and the drop shadow come out — all three existed only to fake
      that blend
  [x] Hero headline is one colour instead of splitting gold/amber mid-phrase

  NOT VERIFIED BY CLAUDE — check on the preview before merging:
  - Fraunces and the palette. Webfonts do not register in the sandbox, so every
    screenshot Claude produced is a fallback serif. Nobody has seen the real
    thing yet
  - That the video actually plays (YouTube is firewalled from the sandbox;
    Aditya confirmed playback separately)
  - The mailto path in a real mail client. Assembled URL is ~1459 chars and
    Outlook historically caps near 2083, so there is headroom but not much

COLLABORATION — scoped to one teammate doing one cleanup pass
  [x] Settings -> Collaborators -> teammate added, invite accepted
  [x] She has Claude Max
  [ ] Set up her Mac yourself — see the appendix at the bottom of EDITING.md.
      Command Line Tools, clone, `gh auth login` AS HER, git user.name/email
      AS HER, then the Claude desktop app. Run the verify block before you
      leave her machine; a broken push discovered mid-edit costs her an evening
  [ ] Send her EDITING.md (she only needs the part above the appendix)
  [ ] Ask her to keep the whole cleanup pass on ONE branch so it lands as ONE
      pull request, not six
  [ ] Review the diff, merge, confirm the site updates (~1 min after merge)

  Editing flow is the Claude desktop app in a Local session on a clone. She
  asks Claude for the git work in plain English; no second app. Chosen over
  Claude Code on the web because a cloud session can't show her the rendered
  page — only a diff. The desktop Browser pane opens index.html directly and
  screenshots its own work.

  Watch for: she has a real clone, so a commit straight to main from her
  machine would deploy without review, and nothing on her screen shows which
  branch she's on. CLAUDE.md now carries a "Branch before committing" rule,
  which loads into her sessions and is the actual guardrail. It keys off
  `git config user.email`, so it binds her and leaves Aditya's direct pushes
  to main alone. Note the corollary: it stops applying if she ever edits from
  a machine configured with Aditya's git identity — one more reason her setup
  authenticates as her. If a stray commit does land, Revert on GitHub fixes it.

  Deliberately skipped for now, revisit if more people join:
  - Branch protection on main. It would block Aditya's own direct pushes too,
    and a stray commit to main is recoverable with a one-click Revert. Slightly
    more tempting now that a second machine can push to main, but the fix is
    still one click and the cost is every push Aditya makes.

CLOUDFLARE BRANCH PREVIEWS — working, one check left
  Goal: a URL per branch, so a branched version can be opened next to the live
  site and compared. A diff can't answer "does this look right."

  Ended up on Cloudflare WORKERS (static assets), not Pages — Cloudflare now
  steers new projects to Workers. Same outcome, different hostnames:
    fight-glare-irvine.aditya-14a.workers.dev      production build
    *-fight-glare-irvine.aditya-14a.workers.dev    branch previews

  ADDITIVE, NOT A MIGRATION. fightglare.org keeps being served by GitHub Pages,
  DNS is not touched, nothing about the live site changes. Reversible by
  deleting the Cloudflare project. This is NOT the migration under LATER.

  [x] Cloudflare account + project connected to the repo
  [x] _headers rewritten for workers.dev (Pages placeholder syntax does not
      match a workers.dev host and would have silently done nothing)
  [x] _headers pushed and built
  [x] Cloudflare's own autoconfig PR (#1) merged
  [x] Branch builds CONFIRMED working
  [x] END-TO-END CONFIRMED Aug 2026. A branch-only string returned 1 from the
      preview URL, which is stronger proof than a 200 — a half-built or stale
      deploy also returns 200. Use that test, not the status code:
        curl -s "<preview-url>/?cb=$RANDOM" | grep -c "<string only on the branch>"
      Slashes in a branch name become dashes in the hostname, and the label has
      to fit in 63 characters — claude/irvine-streetlight-standards-21g9cy came
      out at 61. Keep branch names short.
      Cache-bust and hard-reload; a stale browser copy reads exactly like a
      failed deploy.
  [x] EDITING.md step 6 written against the real URL format
  [x] Preview confirmed serving the real site in a browser (Aditya, Sep 2026),
      not just returning 200. Two URL forms exist:
        <commit-hash>-fight-glare-irvine.aditya-14a.workers.dev   per commit
        <branch-name>-fight-glare-irvine.aditya-14a.workers.dev   per branch
      The per-branch alias is stable but SLOW to update — when it looks stale,
      read the CSS with curl rather than trusting the render, and use the
      per-commit URL for an immediate answer.
  [ ] Verify noindex landed. MUST run from a normal terminal, NOT a Claude
      session — workers.dev is blocked by the sandbox firewall, confirmed
      again this session:
        curl -sI https://fight-glare-irvine.aditya-14a.workers.dev/ | grep -i x-robots-tag
        curl -sI https://fightglare.org/ | grep -i x-robots-tag     # expect NOTHING
      (fightglare.org side already verified: no x-robots-tag, correct.)
  [ ] DO NOT add fightglare.org as a custom domain in Cloudflare. That is the
      single step that would move production off GitHub Pages.

LATER
  [ ] Aditya wants a walkthrough of how GitHub Actions works
  [ ] Possible migration to Cloudflare Pages (faster CDN, preview deploys per
      branch, analytics). Clean to do anytime: change nameservers, connect the
      repo, delete CNAME + .github/workflows/deploy.yml.

GODADDY DNS — FINAL STATE (done, for reference)
  A      @      185.199.108.153
  A      @      185.199.109.153
  A      @      185.199.110.153
  A      @      185.199.111.153
  CNAME  www    arphysics.github.io
