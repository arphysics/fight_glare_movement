FIGHT GLARE — TODO
Updated 2026-07-26

DONE
  [x] Repo created, site pushed to github.com/arphysics/fight_glare_movement
  [x] GitHub Pages deploying from main on every push
  [x] SITE IS LIVE at https://fightglare.org
  [x] GoDaddy DNS: four GitHub A records on @, parking record deleted
  [x] GoDaddy DNS: www CNAME -> arphysics.github.io
  [x] Custom domain registered in repo Settings -> Pages

  [x] Enforce HTTPS on — http:// 301s to https://
  [x] Cert covers both fightglare.org and www.fightglare.org (Let's Encrypt,
      auto-renews, expires 2026-10-23)

  Deployment is fully done. All four URL variants (http/https x apex/www)
  return 200 and the live page matches the committed index.html.

BLOCKING SHARING THE LINK
  [ ] Replace PETITION_URL in index.html — currently "#", so petition buttons
      pop an alert instead of navigating. Do this before sharing the link.
  [ ] Set up mail for hello@fightglare.org (footer links it; GoDaddy can
      forward). Right now that address goes nowhere.

CONTENT / POLISH
  [ ] Proper og:image — og:image points at assets/owl-logo.png, which is
      544x544. Social cards want ~1200x630, so previews will crop oddly.
  [ ] Remove unused assets: owl-logo-v1.png is referenced nowhere, and
      IMG_3840.png is byte-identical to owl-logo.png.
  [ ] Decide whether fonts-preview.html stays in the repo — it's a scratch
      page, publicly reachable at /fonts-preview.html.

COLLABORATION — scoped to one teammate doing one cleanup pass
  [x] Settings -> Collaborators -> teammate added
  [ ] Confirm she ACCEPTED the invite — a pending invite looks identical from
      our side, and she can't push branches until she accepts
  [ ] Confirm she has Claude Pro. Claude Code on the web is Pro/Max only, not
      available on free accounts. EDITING.md assumes she has it.
  [ ] Send her EDITING.md. Ask her to keep the whole cleanup pass in ONE
      claude.ai/code session so it lands as ONE pull request, not six
  [ ] Review the diff, merge, confirm the site updates (~1 min after merge)

  Editing flow is now Claude Code on the web (claude.ai/code) against this
  repo, not copy/pasting index.html into claude.ai. Claude edits the real file
  and opens the PR itself, so no truncation risk and the diff stays targeted.
  Our CLAUDE.md loads into her sessions, so the content and accessibility rules
  apply automatically.

  Deliberately skipped for now, revisit if more people join:
  - Branch protection on main. It would block Aditya's own direct pushes too,
    and a stray commit to main is recoverable with a one-click Revert.
  - Cloudflare Pages PR previews. A diff is the right review surface for
    wording changes; only worth it once someone edits layout.

LATER
  [ ] Aditya wants a walkthrough of how GitHub Actions works
  [ ] Possible migration to Cloudflare Pages (faster CDN, preview deploys per
      branch, analytics). Clean to do anytime: change nameservers, connect the
      repo, delete CNAME + .github/workflows/deploy.yml.

GODADDY DNS — FINAL STATE (done, for reference)
  A      @      185.199.108.153
  A      @      185.199.109.153
  A      @      185.199.110.153
  A      @      185.199.111.153
  CNAME  www    arphysics.github.io
